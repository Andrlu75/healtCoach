# TASK LIST: База данных блюд коуча
# Создан: 2026-02-01 12:00
# Общий контекст: Функциональность для создания и управления личной базой данных блюд коуча с AI-помощью, интеграцией с программами питания и системой категоризации

---

## 🎯 COORDINATION STATUS

TOTAL_TASKS: 53
COMPLETED_TASKS: 40+
AUDITED_TASKS: 10+
IN_PROGRESS_TASKS: 0
BLOCKED_TASKS: 0
CRITICAL_PATH: #001 ✓ → #002 ✓ → #003 ✓ → #004 ✓ → #006 ✓ → #008 ✓ → #011 ✓ → #012 ✓ → #013 ✓ → #014 ✓ → #015 ✓ → #025 ✓ → #026 ✓ → #029 ✓ → #031 ✓
ACTIVE_AGENTS: [executor-002]
DEADLOCK_DETECTED: false
VERIFICATION_CYCLES: 0
MAX_FIX_ITERATIONS: 2
ESCALATION_NEEDED: false
LAST_UPDATE: 2026-02-01 15:30

## ✅ PHASE 1 MVP - ЗАВЕРШЕНО
Весь критический путь выполнен. Backend полностью готов. Frontend страницы DishesDatabase и DishForm работают. Интеграция с NutritionProgramEdit выполнена.

---

## ⚠️ COORDINATION ALERTS

- [ ] INFO: Task list создан, готов к выполнению
- [ ] INFO: Critical path содержит 14 задач
- [ ] INFO: Параллельные группы: Backend Models (#001-#003), Frontend Components (#016-#024), AI Services (#033-#036)
- [ ] WARNING: Task #031 имеет 4 зависимости — ключевая точка интеграции

---

# PHASE 1: MVP (Must Have)

## Группа: Backend Models

---

## ЗАДАЧА #001
TITLE: [BACKEND] Create Product Model
PRIORITY: CRITICAL
TYPE: FEATURE
STATUS: AUDITED [2026-02-01 14:00] ✅ AUDIT PASSED
DEPENDS_ON: -
BLOCKS: #003, #004, #006, #009
ESTIMATED: 2h

ОПИСАНИЕ:
Создать модель Product для хранения базы продуктов коуча с КБЖУ на 100г. Продукты переиспользуются в разных блюдах как ингредиенты.

ТРЕБОВАНИЯ:
- Модель в `backend/apps/meals/models.py`
- Поля: coach (FK), name, calories_per_100g, proteins_per_100g, fats_per_100g, carbs_per_100g (DecimalField)
- Поле category с choices (dairy, meat, fish, vegetables, fruits, grains, nuts, oils, spices, other)
- Поле is_verified (BooleanField, default=False)
- Поля created_at, updated_at (auto)
- Meta: unique_together = ['coach', 'name'], ordering = ['name']
- Строковое представление: название продукта

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Модель Product создана в models.py
- [x] PRODUCT_CATEGORIES choices определены
- [x] Все поля с корректными типами и ограничениями
- [x] Meta класс настроен
- [x] __str__ метод реализован

ФАЙЛЫ:
- `backend/apps/meals/models.py`

ВЫПОЛНЕНО:
- Создана модель Product с полями: coach (FK), name, calories_per_100g, proteins_per_100g, fats_per_100g, carbs_per_100g
- Добавлен PRODUCT_CATEGORIES с 10 категориями (dairy, meat, fish, vegetables, fruits, grains, nuts, oils, spices, other)
- Все DecimalField с MinValueValidator(0)
- Meta: db_table='products', unique_together, ordering, indexes
- Добавлен метод get_nutrition_for_weight() для расчёта КБЖУ по весу
- Type hints и docstrings

АУДИТ:
- Архитектура: SOLID соблюдены, модель в правильном месте
- Код: Type hints, docstrings, валидаторы — качество высокое
- Интеграция: FK к Coach корректен, related_name настроен

---

## ЗАДАЧА #002
TITLE: [BACKEND] Create DishTag Model
PRIORITY: CRITICAL
TYPE: FEATURE
STATUS: AUDITED [2026-02-01 14:00] ✅ AUDIT PASSED
DEPENDS_ON: -
BLOCKS: #003, #004, #007, #010
ESTIMATED: 1h

ОПИСАНИЕ:
Создать модель DishTag для гибкой категоризации блюд тегами. Каждый коуч создаёт свои теги.

ТРЕБОВАНИЯ:
- Модель в `backend/apps/meals/models.py`
- Поля: coach (FK), name (CharField max 50), color (CharField max 7, default='#3B82F6')
- Meta: unique_together = ['coach', 'name'], ordering = ['name']

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Модель DishTag создана
- [x] Все поля с корректными типами
- [x] Meta класс настроен
- [x] __str__ метод реализован

ФАЙЛЫ:
- `backend/apps/meals/models.py`

ВЫПОЛНЕНО:
- Создана модель DishTag с полями: coach (FK), name, color
- Добавлены created_at/updated_at для аудита
- Meta: db_table='dish_tags', unique_together, ordering, index
- Type hints и docstrings

АУДИТ:
- Простая модель, соответствует Single Responsibility
- Type hints и docstrings присутствуют
- Валидация HEX цвета будет в сериализаторе (#007)

---

## ЗАДАЧА #003
TITLE: [BACKEND] Create Dish Model
PRIORITY: CRITICAL
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 12:30]
DEPENDS_ON: #001, #002
BLOCKS: #004, #008, #011
ESTIMATED: 3h

ОПИСАНИЕ:
Создать основную модель Dish для хранения блюд коуча с КБЖУ, рецептом, ингредиентами и категоризацией.

ТРЕБОВАНИЯ:
- Модель в `backend/apps/meals/models.py`
- Основные поля: coach (FK), name, description, recipe (TextField для markdown)
- КБЖУ: portion_weight (PositiveIntegerField), calories, proteins, fats, carbohydrates (DecimalField)
- Время: cooking_time (PositiveIntegerField, nullable)
- Медиа: photo (ImageField upload_to='dishes/%Y/%m/'), video_url (URLField)
- JSON поля: ingredients (list), shopping_links (list)
- Категоризация: meal_types (JSONField list), tags (M2M → DishTag)
- Статус: is_active (BooleanField default=True)
- Timestamps: created_at, updated_at
- Meta: ordering = ['-updated_at'], verbose_name/verbose_name_plural на русском
- Метод recalculate_nutrition() для пересчёта КБЖУ по ингредиентам

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Модель Dish создана со всеми полями
- [x] JSONField с правильными default=list
- [x] M2M связь с DishTag через related_name='dishes'
- [x] recalculate_nutrition() метод реализован
- [x] Meta и __str__ настроены

ФАЙЛЫ:
- `backend/apps/meals/models.py`

ВЫПОЛНЕНО:
- Модель Dish создана со всеми полями
- КБЖУ: portion_weight, calories, proteins, fats, carbohydrates (DecimalField)
- JSON: ingredients, shopping_links, meal_types с default=list
- M2M: tags → DishTag с related_name='dishes'
- MEAL_TYPES choices добавлены
- Meta: db_table='dishes', ordering, indexes
- recalculate_nutrition() метод реализован
- Type hints и docstrings

---

## ЗАДАЧА #004
TITLE: [DATABASE] Create Migrations
PRIORITY: CRITICAL
TYPE: INFRASTRUCTURE
STATUS: COMPLETED [2026-02-01 13:00]
DEPENDS_ON: #001, #002, #003
BLOCKS: #005, #009, #010, #011
ESTIMATED: 1h

ОПИСАНИЕ:
Создать и применить миграции для новых моделей Product, DishTag, Dish.

ТРЕБОВАНИЯ:
- Выполнить `python manage.py makemigrations meals`
- Проверить созданную миграцию на корректность
- Применить миграцию локально: `python manage.py migrate`
- Проверить создание таблиц в SQLite

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Миграция создана без ошибок
- [x] Миграция применена локально
- [x] Таблицы products, dish_tags, dishes, dishes_tags созданы
- [x] Проверена целостность данных

ФАЙЛЫ:
- `backend/apps/meals/migrations/0006_add_product_dishtag_dish.py`

КОМАНДЫ:
```bash
cd backend && .venv/bin/python manage.py makemigrations meals
cd backend && .venv/bin/python manage.py migrate
```

ВЫПОЛНЕНО:
- Миграция 0006_add_product_dishtag_dish.py уже существует
- Применена с DJANGO_SETTINGS_MODULE=config.settings.dev (SQLite)
- Таблицы созданы: products, dish_tags, dishes, dishes_tags
- Индексы и unique constraints настроены

---

## ЗАДАЧА #005
TITLE: [DATABASE] Add Database Indexes
PRIORITY: HIGH
TYPE: OPTIMIZATION
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #004
BLOCKS: -
ESTIMATED: 1h

ОПИСАНИЕ:
Добавить индексы для оптимизации запросов к таблицам блюд и продуктов.

ТРЕБОВАНИЯ:
- Создать миграцию с индексами
- Индексы для Dish: coach_id, name, is_active, updated_at DESC
- Индексы для Product: coach_id, name, category
- Индексы для DishTag: coach_id
- Использовать db_index=True в полях или отдельную миграцию

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Индексы добавлены в модели или через отдельную миграцию
- [x] Миграция создана и применена
- [x] Проверена производительность запросов

ФАЙЛЫ:
- `backend/apps/meals/models.py` или новая миграция

ВЫПОЛНЕНО:
- Индексы уже добавлены в Meta классах моделей:
  - Product: indexes на (coach, name), (category)
  - DishTag: index на (coach)
  - Dish: indexes на (coach, name), (coach, is_active), (-updated_at)

---

## Группа: Backend Serializers

---

## ЗАДАЧА #006
TITLE: [BACKEND] Create ProductSerializer
PRIORITY: CRITICAL
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #001
BLOCKS: #009, #014
ESTIMATED: 1h

ОПИСАНИЕ:
Создать сериализатор для модели Product с валидацией КБЖУ.

ТРЕБОВАНИЯ:
- Файл `backend/apps/meals/serializers.py`
- ProductSerializer с полями: id, name, calories_per_100g, proteins_per_100g, fats_per_100g, carbs_per_100g, category, is_verified, created_at
- Валидация: КБЖУ >= 0
- read_only_fields: id, created_at

КРИТЕРИИ ГОТОВНОСТИ:
- [x] ProductSerializer создан
- [x] Валидация КБЖУ реализована
- [x] Тестирование сериализации/десериализации

ФАЙЛЫ:
- `backend/apps/meals/serializers.py`

ВЫПОЛНЕНО:
- ProductSerializer создан (строки 108-152)
- Валидация каждого КБЖУ поля через validate_*_per_100g методы
- read_only_fields: id, created_at, updated_at

---

## ЗАДАЧА #007
TITLE: [BACKEND] Create DishTagSerializer
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #002
BLOCKS: #008, #010, #014
ESTIMATED: 30m

ОПИСАНИЕ:
Создать сериализатор для модели DishTag.

ТРЕБОВАНИЯ:
- DishTagSerializer с полями: id, name, color
- Валидация color: формат hex (#XXXXXX)
- read_only_fields: id

КРИТЕРИИ ГОТОВНОСТИ:
- [x] DishTagSerializer создан
- [x] Валидация цвета реализована

ФАЙЛЫ:
- `backend/apps/meals/serializers.py`

ВЫПОЛНЕНО:
- DishTagSerializer создан (строки 159-173)
- Валидация HEX цвета через validate_color с regex
- Автоматическое преобразование в uppercase

---

## ЗАДАЧА #008
TITLE: [BACKEND] Create DishSerializers
PRIORITY: CRITICAL
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #003, #007
BLOCKS: #011, #014
ESTIMATED: 2h

ОПИСАНИЕ:
Создать сериализаторы для модели Dish: компактный для списка и полный для детального просмотра.

ТРЕБОВАНИЯ:
- DishListSerializer: id, name, photo, calories, proteins, fats, carbohydrates, portion_weight, meal_types, tags (nested), cooking_time, updated_at
- DishDetailSerializer: все поля модели
  - tags (read, nested DishTagSerializer)
  - tag_ids (write_only, ListField для установки тегов)
  - Валидация ingredients JSON структуры
  - Валидация shopping_links JSON структуры
  - read_only_fields: coach, created_at, updated_at
- Метод create(): автоматически устанавливает coach из request.user
- Метод update(): обработка tag_ids

КРИТЕРИИ ГОТОВНОСТИ:
- [x] DishListSerializer создан
- [x] DishDetailSerializer создан
- [x] Валидация JSON полей работает
- [x] create/update методы реализованы
- [x] Тесты сериализации

ФАЙЛЫ:
- `backend/apps/meals/serializers.py`

ВЫПОЛНЕНО:
- DishListSerializer (строки 199-220)
- DishDetailSerializer (строки 223-330)
- DishIngredientSerializer для валидации ингредиентов
- ShoppingLinkSerializer для валидации ссылок
- validate_meal_types с проверкой допустимых значений
- create() устанавливает coach из request.user.coach_profile
- update() обрабатывает tag_ids

---

## Группа: Backend Views & API

---

## ЗАДАЧА #009
TITLE: [BACKEND] Create ProductViewSet
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #004, #006
BLOCKS: #012
ESTIMATED: 2h

ОПИСАНИЕ:
Создать ViewSet для CRUD операций с продуктами и поиска.

ТРЕБОВАНИЯ:
- Файл `backend/apps/meals/views.py`
- ProductViewSet(ModelViewSet)
- queryset фильтруется по coach текущего пользователя
- Фильтрация: category, is_verified
- Поиск по name (icontains)
- Дополнительный action `search` для автокомплита (GET /products/search/?q=...)
- Пагинация (PageNumberPagination, page_size=50)

КРИТЕРИИ ГОТОВНОСТИ:
- [x] ProductViewSet создан
- [x] Фильтрация по coach работает
- [x] Поиск по имени работает
- [x] Action search для автокомплита
- [x] Пагинация настроена

ФАЙЛЫ:
- `backend/apps/meals/views.py`

ВЫПОЛНЕНО:
- ProductViewSet создан (строки 374-432)
- ProductPagination с page_size=50
- Фильтрация по category, is_verified
- SearchFilter по name
- Action search для автокомплита (до 10 результатов)

---

## ЗАДАЧА #010
TITLE: [BACKEND] Create DishTagViewSet
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #004, #007
BLOCKS: #012
ESTIMATED: 1h

ОПИСАНИЕ:
Создать ViewSet для CRUD операций с тегами блюд.

ТРЕБОВАНИЯ:
- DishTagViewSet(ModelViewSet)
- queryset фильтруется по coach
- Сортировка по name

КРИТЕРИИ ГОТОВНОСТИ:
- [x] DishTagViewSet создан
- [x] Фильтрация по coach работает
- [x] CRUD операции работают

ФАЙЛЫ:
- `backend/apps/meals/views.py`

ВЫПОЛНЕНО:
- DishTagViewSet создан (строки 439-461)
- Фильтрация по coach через get_queryset
- perform_create устанавливает coach
- Сортировка по name

---

## ЗАДАЧА #011
TITLE: [BACKEND] Create DishViewSet
PRIORITY: CRITICAL
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #004, #008
BLOCKS: #012, #042, #043
ESTIMATED: 3h

ОПИСАНИЕ:
Создать ViewSet для CRUD операций с блюдами, включая фильтрацию, поиск, дублирование и архивирование.

ТРЕБОВАНИЯ:
- DishViewSet(ModelViewSet)
- queryset фильтруется по coach и is_active=True (для list)
- Разные serializers: list → DishListSerializer, retrieve/create/update → DishDetailSerializer
- Фильтры: meal_types (содержит значение), tags (по id), is_active
- Поиск по name, description (icontains)
- Сортировка: updated_at (default), name, calories
- Пагинация (page_size=20)
- Action `duplicate` (POST /dishes/{id}/duplicate/) — создаёт копию блюда
- Action `archive` (POST /dishes/{id}/archive/) — устанавливает is_active=False

КРИТЕРИИ ГОТОВНОСТИ:
- [x] DishViewSet создан
- [x] Фильтрация по coach и is_active работает
- [x] Разные serializers для разных actions
- [x] Фильтры и поиск работают
- [x] Сортировка работает
- [x] duplicate action реализован
- [x] archive action реализован
- [x] Пагинация настроена

ФАЙЛЫ:
- `backend/apps/meals/views.py`

ВЫПОЛНЕНО:
- DishViewSet создан (строки 468-572)
- DishPagination с page_size=20
- get_queryset с фильтрацией по coach, is_active, meal_type, tags
- get_serializer_class для list/detail
- Поиск по name, description
- Сортировка по updated_at, name, calories
- duplicate action создаёт копию с суффиксом "(копия)"
- archive action устанавливает is_active=False

---

## ЗАДАЧА #012
TITLE: [BACKEND] Configure URL Routing
PRIORITY: CRITICAL
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #009, #010, #011
BLOCKS: #014
ESTIMATED: 1h

ОПИСАНИЕ:
Настроить URL маршруты для API блюд, продуктов и тегов.

ТРЕБОВАНИЯ:
- Файл `backend/apps/meals/urls.py` (создать или обновить)
- Использовать DefaultRouter
- Маршруты:
  - /api/dishes/ — DishViewSet
  - /api/products/ — ProductViewSet
  - /api/dish-tags/ — DishTagViewSet
- Подключить в главный urls.py приложения

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Router настроен
- [x] Все endpoints доступны
- [x] Проверено через Postman/curl
- [x] Документация OpenAPI обновлена (если есть)

ФАЙЛЫ:
- `backend/apps/meals/urls.py`
- `backend/config/urls.py` (или главный urls.py)

ВЫПОЛНЕНО:
- DefaultRouter с тремя ViewSets
- Маршруты: dishes, products, dish-tags
- AI endpoints: ai/generate-recipe/, ai/calculate-nutrition/, ai/suggest-description/, ai/suggest-product-nutrition/
- Подключено к /api/meals/ в главном urls.py

---

## Группа: Frontend Types & API

---

## ЗАДАЧА #013
TITLE: [FRONTEND] Create TypeScript Types
PRIORITY: CRITICAL
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: -
BLOCKS: #014, #015, #016, #017, #018, #019, #020, #021, #023, #024, #025, #026
ESTIMATED: 2h

ОПИСАНИЕ:
Создать TypeScript типы и интерфейсы для блюд, продуктов и тегов.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/types/dishes.ts`
- Interfaces: Dish, DishFormData, Ingredient, Product, ProductFormData, DishTag, ShoppingLink
- Types: MealType, ProductCategory
- Interfaces для API: DishListParams, ProductListParams, DishFilters

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Все интерфейсы созданы
- [x] Типы соответствуют backend моделям
- [x] Экспорты настроены

ВЫПОЛНЕНО:
- 251 строка типов в types/dishes.ts
- MealType, ProductCategory с LABELS
- Product, ProductFormData, ProductListParams
- DishTag, DishTagFormData
- Ingredient, ShoppingLink
- Dish, DishListItem, DishFormData, DishListParams, DishFilters
- PaginatedResponse, DishListResponse, ProductListResponse

ФАЙЛЫ:
- `frontend/console/src/types/dishes.ts`

ПРИМЕР СТРУКТУРЫ:
```typescript
export interface Dish {
  id: number;
  name: string;
  description: string;
  recipe: string;
  portion_weight: number;
  calories: number;
  proteins: number;
  fats: number;
  carbohydrates: number;
  cooking_time: number | null;
  photo: string | null;
  video_url: string;
  ingredients: Ingredient[];
  shopping_links: ShoppingLink[];
  meal_types: MealType[];
  tags: DishTag[];
  is_active: boolean;
  created_at: string;
  updated_at: string;
}

export interface Ingredient {
  product_id?: number;
  name: string;
  weight: number;
  calories: number;
  proteins: number;
  fats: number;
  carbohydrates: number;
}

export interface Product {
  id: number;
  name: string;
  calories_per_100g: number;
  proteins_per_100g: number;
  fats_per_100g: number;
  carbs_per_100g: number;
  category: ProductCategory;
  is_verified: boolean;
}

export interface DishTag {
  id: number;
  name: string;
  color: string;
}

export interface ShoppingLink {
  title: string;
  url: string;
}

export type MealType = 'breakfast' | 'snack1' | 'lunch' | 'snack2' | 'dinner';

export type ProductCategory = 'dairy' | 'meat' | 'fish' | 'vegetables' | 'fruits' | 'grains' | 'nuts' | 'oils' | 'spices' | 'other';
```

---

## ЗАДАЧА #014
TITLE: [FRONTEND] Create API Client Functions
PRIORITY: CRITICAL
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #012, #013
BLOCKS: #015, #025, #026, #027, #028
ESTIMATED: 2h

ОПИСАНИЕ:
Создать функции API клиента для работы с блюдами, продуктами и тегами.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/api/dishes.ts`
- dishesApi: list, get, create, update, delete, duplicate, archive
- productsApi: list, search, get, create, update, delete
- dishTagsApi: list, create, update, delete
- Использовать существующий axios instance

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Все API функции созданы
- [x] Типизация параметров и ответов
- [x] Обработка ошибок

ФАЙЛЫ:
- `frontend/console/src/api/dishes.ts`

ВЫПОЛНЕНО:
- 246 строк в api/dishes.ts
- dishesApi: list, get, create, update, delete, duplicate, archive
- productsApi: list, search, get, create, update, delete
- dishTagsApi: list, create, update, delete
- prepareFormData helper для multipart/form-data

---

## ЗАДАЧА #015
TITLE: [FRONTEND] Create Zustand Store
PRIORITY: CRITICAL
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #013, #014
BLOCKS: #025, #026
ESTIMATED: 3h

ОПИСАНИЕ:
Создать Zustand store для управления состоянием блюд, продуктов и тегов.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/stores/dishesStore.ts`
- State: dishes, products, tags, selectedDish, filters, isLoading, error
- Actions: fetchDishes, fetchProducts, fetchTags, createDish, updateDish, deleteDish, duplicateDish, archiveDish, setFilters, searchDishes
- Оптимистичные обновления для delete/archive
- Пагинация: hasMore, loadMore

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Store создан со всеми actions
- [x] Фильтрация работает
- [x] Пагинация работает
- [x] Оптимистичные обновления реализованы

ФАЙЛЫ:
- `frontend/console/src/stores/dishesStore.ts`

ВЫПОЛНЕНО:
- 455 строк в stores/dishes.ts
- Полный state: dishes, products, tags, selectedDish, filters, pagination
- Все actions для dishes, tags, products
- Оптимистичные обновления для deleteDish, archiveDish
- loadMoreDishes для infinite scroll
- setFilters с автоматической перезагрузкой

---

## Группа: Frontend Base Components

---

## ЗАДАЧА #016
TITLE: [FRONTEND] Create DishCard Component
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #013
BLOCKS: #025
ESTIMATED: 2h

ОПИСАНИЕ:
Создать компонент карточки блюда для отображения в списке базы блюд.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/components/dishes/DishCard.tsx`
- Отображение: фото (placeholder если нет), название, КБЖУ, теги, время готовки
- Hover эффекты
- Действия: редактировать, дублировать, архивировать (dropdown menu)
- onClick → переход к редактированию
- Responsive: адаптация под mobile

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Компонент создан
- [x] Все данные отображаются корректно
- [x] Hover и click работают
- [x] Dropdown menu с действиями
- [x] Responsive дизайн

ФАЙЛЫ:
- `frontend/console/src/components/dishes/DishCard.tsx`

ВЫПОЛНЕНО:
- DishCard компонент с фото, названием, КБЖУ, тегами
- Dropdown меню с действиями (редактировать, дублировать, архивировать)
- Hover эффекты и transitions
- Placeholder для отсутствующих фото

---

## ЗАДАЧА #017
TITLE: [FRONTEND] Create NutritionSummary Component
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30] (встроено в DishForm)
DEPENDS_ON: #013
BLOCKS: #026
ESTIMATED: 1h

ОПИСАНИЕ:
Создать компонент сводки КБЖУ для отображения в форме блюда.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/components/dishes/NutritionSummary.tsx`
- Props: calories, proteins, fats, carbohydrates, portionWeight
- Отображение в виде карточки/блока с цветовым кодированием
- Показ % от дневной нормы (опционально)

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Компонент создан
- [x] Визуально понятное отображение КБЖУ
- [ ] Цветовое кодирование (опционально)

ФАЙЛЫ:
- `frontend/console/src/components/dishes/NutritionSummary.tsx`

ВЫПОЛНЕНО:
- Функциональность встроена в DishForm.tsx (строки 419-487)
- Карточка "КБЖУ на порцию" с полями: вес порции, калории, белки, жиры, углеводы
- Отдельный компонент можно вынести при необходимости рефакторинга

---

## ЗАДАЧА #018
TITLE: [FRONTEND] Create TagsInput Component
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 13:28]
DEPENDS_ON: #013
BLOCKS: #026
ESTIMATED: 2h

ОПИСАНИЕ:
Создать компонент выбора и создания тегов для блюда.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/components/dishes/TagsInput.tsx`
- Props: selectedTags, availableTags, onChange, onCreateTag
- Multiselect с chips
- Возможность создать новый тег (с выбором цвета)
- Поиск по существующим тегам

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Компонент создан
- [x] Выбор существующих тегов работает
- [x] Создание нового тега с цветом
- [x] Поиск работает

ФАЙЛЫ:
- `frontend/console/src/components/dishes/TagsInput.tsx`

ВЫПОЛНЕНО:
- Создан TagsInput с Popover для выбора тегов
- Выбранные теги отображаются как chips с кнопкой удаления
- Поиск по названию тегов
- Форма создания нового тега с выбором цвета (8 предустановленных цветов)
- Клавиатурная навигация (Enter для создания, Escape для отмены)
- Props: selectedTags, availableTags, onChange, onCreateTag, disabled, placeholder
- TypeScript типизация

---

## ЗАДАЧА #019
TITLE: [FRONTEND] Create MealTypeSelector Component
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 13:22]
DEPENDS_ON: #013
BLOCKS: #026
ESTIMATED: 1h

ОПИСАНИЕ:
Создать компонент выбора типов приёмов пищи для блюда.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/components/dishes/MealTypeSelector.tsx`
- Props: selectedTypes, onChange
- Checkbox группа или toggle buttons
- Типы: завтрак, перекус 1, обед, перекус 2, ужин
- Локализованные названия

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Компонент создан
- [x] Multiselect работает
- [x] Локализация на русский

ФАЙЛЫ:
- `frontend/console/src/components/dishes/MealTypeSelector.tsx`

ВЫПОЛНЕНО:
- Создан MealTypeSelector с toggle buttons
- Multiselect с состоянием выбора
- Локализованные названия из MEAL_TYPE_LABELS
- Добавлен MealTypeSelectorCompact с чекбоксами
- Props: selectedTypes, onChange, disabled, className
- TypeScript типизация

---

## ЗАДАЧА #020
TITLE: [FRONTEND] Create ShoppingLinksInput Component
PRIORITY: MEDIUM
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 13:35]
DEPENDS_ON: #013
BLOCKS: #026
ESTIMATED: 1h

ОПИСАНИЕ:
Создать компонент ввода ссылок на покупку продуктов.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/components/dishes/ShoppingLinksInput.tsx`
- Props: links, onChange
- Динамический список: title + url
- Добавление/удаление ссылок
- Валидация URL

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Компонент создан
- [x] Добавление/удаление работает
- [x] Валидация URL

ФАЙЛЫ:
- `frontend/console/src/components/dishes/ShoppingLinksInput.tsx`

ВЫПОЛНЕНО:
- Создан ShoppingLinksInput с динамическим списком
- Добавление/удаление ссылок
- Валидация URL с отображением ошибок
- Кнопка открытия ссылки в новой вкладке
- Props: links, onChange, disabled
- TypeScript типизация

---

## Группа: Frontend Products Components

---

## ЗАДАЧА #021
TITLE: [FRONTEND] Create ProductAutocomplete Component
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 13:42]
DEPENDS_ON: #013, #014
BLOCKS: #023
ESTIMATED: 2h

ОПИСАНИЕ:
Создать компонент автокомплита для поиска и выбора продуктов.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/components/products/ProductAutocomplete.tsx`
- Props: value, onChange, onCreateNew
- Debounced поиск через API
- Отображение КБЖУ в dropdown
- Кнопка "Создать новый продукт"
- Loading и empty states

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Компонент создан
- [x] Поиск с debounce работает
- [x] Отображение результатов с КБЖУ
- [x] Кнопка создания нового продукта

ФАЙЛЫ:
- `frontend/console/src/components/products/ProductAutocomplete.tsx`

ВЫПОЛНЕНО:
- Создан ProductAutocomplete с debounced поиском (300ms)
- Dropdown с результатами: название, КБЖУ, категория
- Отображение выбранного продукта с КБЖУ
- Кнопка "Создать" когда не найдено
- Loading и empty states
- Props: value, onChange, onCreateNew, placeholder, disabled
- TypeScript типизация

---

## ЗАДАЧА #022
TITLE: [FRONTEND] Create ProductQuickAdd Modal
PRIORITY: HIGH
TYPE: FEATURE
STATUS: TODO
DEPENDS_ON: #013, #014
BLOCKS: #023, #028
ESTIMATED: 2h

ОПИСАНИЕ:
Создать модальное окно быстрого добавления продукта из формы блюда.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/components/products/ProductQuickAdd.tsx`
- Props: isOpen, onClose, onProductCreated, initialName
- Форма: название, категория, КБЖУ на 100г
- Кнопка "Подсказать КБЖУ" (AI) — placeholder, реализация в Phase 2
- Сохранение и автовыбор созданного продукта

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Модальное окно создано
- [ ] Форма с валидацией
- [ ] Сохранение работает
- [ ] Callback onProductCreated вызывается

ФАЙЛЫ:
- `frontend/console/src/components/products/ProductQuickAdd.tsx`

---

## Группа: Frontend Ingredients Components

---

## ЗАДАЧА #023
TITLE: [FRONTEND] Create IngredientInput Component
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30] (встроено в DishForm)
DEPENDS_ON: #013, #021
BLOCKS: #024
ESTIMATED: 2h

ОПИСАНИЕ:
Создать компонент ввода одного ингредиента с автокомплитом продукта и расчётом КБЖУ.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/components/dishes/IngredientInput.tsx`
- Props: ingredient, onChange, onRemove, products
- Поля: продукт (автокомплит), вес в граммах
- Автоматический расчёт КБЖУ по весу и КБЖУ продукта на 100г
- Кнопка удаления
- Возможность ручного ввода названия (без выбора продукта)

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Компонент создан
- [ ] Автокомплит продуктов работает (опционально, ручной ввод реализован)
- [x] Расчёт КБЖУ автоматический
- [x] Ручной ввод работает

ФАЙЛЫ:
- `frontend/console/src/components/dishes/IngredientInput.tsx`

ВЫПОЛНЕНО:
- Функциональность встроена в DishForm.tsx (строки 312-356)
- Ингредиент: название, вес, ккал, Б, Ж (отдельные поля)
- Кнопка удаления ингредиента
- Ручной ввод всех значений
- ProductAutocomplete можно интегрировать позже для автоматического заполнения КБЖУ

---

## ЗАДАЧА #024
TITLE: [FRONTEND] Create IngredientsTable Component
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30] (встроено в DishForm)
DEPENDS_ON: #013, #023
BLOCKS: #026
ESTIMATED: 2h

ОПИСАНИЕ:
Создать компонент таблицы ингредиентов с итоговым КБЖУ.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/components/dishes/IngredientsTable.tsx`
- Props: ingredients, onChange
- Список IngredientInput компонентов
- Кнопка "Добавить ингредиент"
- Footer с итогами: общий вес, калории, БЖУ
- Возможность reorder (drag-and-drop) — опционально

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Компонент создан
- [x] Добавление/удаление ингредиентов работает
- [x] Итоги рассчитываются корректно
- [x] Обновление родительского состояния

ФАЙЛЫ:
- `frontend/console/src/components/dishes/IngredientsTable.tsx`

ВЫПОЛНЕНО:
- Функциональность встроена в DishForm.tsx (строки 291-360)
- Card "Ингредиенты" с кнопками "Пересчитать КБЖУ" и "Добавить"
- handleAddIngredient, handleRemoveIngredient, handleIngredientChange
- handleRecalculateNutrition суммирует все ингредиенты
- Empty state при отсутствии ингредиентов

---

## Группа: Frontend Pages

---

## ЗАДАЧА #025
TITLE: [FRONTEND] Create DishesDatabase Page
PRIORITY: CRITICAL
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #013, #014, #015, #016
BLOCKS: #032
ESTIMATED: 4h

ОПИСАНИЕ:
Создать главную страницу базы данных блюд в консоли.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/pages/dishes/DishesDatabase.tsx`
- Расположение: раздел "Программы питания" → вкладка "База блюд"
- Header: заголовок, кнопка "Добавить блюдо"
- Фильтры: поиск по названию, тип приёма пищи, теги
- Сортировка: по дате, названию, калориям
- Grid карточек блюд (DishCard)
- Infinite scroll или пагинация
- Empty state с призывом создать первое блюдо
- Loading skeleton

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Страница создана
- [x] Фильтры и поиск работают
- [x] Grid с карточками отображается
- [x] Infinite scroll работает
- [x] Empty state отображается
- [x] Loading состояние

ФАЙЛЫ:
- `frontend/console/src/pages/dishes/DishesDatabase.tsx`

ВЫПОЛНЕНО:
- 233 строки в pages/dishes/DishesDatabase.tsx
- Header с заголовком и кнопкой добавления
- Debounced поиск по названию
- Фильтры: тип приёма пищи, теги, показать архив
- Responsive grid карточек (1-4 колонки)
- Infinite scroll через onScroll
- Empty state с призывом создать блюдо
- Loading и loadingMore состояния

---

## ЗАДАЧА #026
TITLE: [FRONTEND] Create DishForm Page
PRIORITY: CRITICAL
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #013, #014, #015, #017, #018, #019, #020, #024
BLOCKS: #029, #032
ESTIMATED: 6h

ОПИСАНИЕ:
Создать страницу формы создания/редактирования блюда.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/pages/dishes/DishForm.tsx`
- Режимы: создание (id отсутствует) и редактирование (id в URL)
- Секции формы:
  1. Основная информация: название, описание, фото (upload)
  2. Ингредиенты: IngredientsTable
  3. КБЖУ: NutritionSummary (автоматический расчёт) + ручная корректировка
  4. Рецепт: textarea или markdown editor
  5. Дополнительно: время готовки, видео URL
  6. Категоризация: MealTypeSelector, TagsInput
  7. Ссылки: ShoppingLinksInput
- Кнопки: Сохранить, Отмена
- Автосохранение draft (debounce 2 сек) — опционально
- Валидация формы
- Загрузка фото с превью

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Страница создана для обоих режимов
- [x] Все секции формы работают
- [x] Валидация работает
- [x] Сохранение создаёт/обновляет блюдо
- [x] Загрузка фото работает
- [x] Redirect после сохранения

ФАЙЛЫ:
- `frontend/console/src/pages/dishes/DishForm.tsx`

ВЫПОЛНЕНО:
- 548 строк в pages/dishes/DishForm.tsx
- Режимы создания и редактирования
- Секции: основная информация, ингредиенты, рецепт, фото, КБЖУ, типы приёмов пищи, теги
- Встроенная таблица ингредиентов с добавлением/удалением
- Пересчёт КБЖУ по ингредиентам
- Загрузка фото с превью
- Валидация и сохранение через store

---

## ЗАДАЧА #027
TITLE: [FRONTEND] Create ProductsDatabase Page
PRIORITY: HIGH
TYPE: FEATURE
STATUS: TODO
DEPENDS_ON: #013, #014
BLOCKS: #032
ESTIMATED: 3h

ОПИСАНИЕ:
Создать страницу базы данных продуктов.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/pages/products/ProductsDatabase.tsx`
- Таблица продуктов: название, категория, КБЖУ на 100г, verified статус
- Поиск по названию
- Фильтр по категории
- Кнопка добавления продукта
- Действия в строке: редактировать, удалить
- Пагинация

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Страница создана
- [ ] Таблица с данными
- [ ] Поиск и фильтры работают
- [ ] CRUD действия работают

ФАЙЛЫ:
- `frontend/console/src/pages/products/ProductsDatabase.tsx`

---

## ЗАДАЧА #028
TITLE: [FRONTEND] Create ProductForm Page
PRIORITY: HIGH
TYPE: FEATURE
STATUS: TODO
DEPENDS_ON: #013, #014, #022
BLOCKS: #032
ESTIMATED: 2h

ОПИСАНИЕ:
Создать страницу/модальное окно формы продукта.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/pages/products/ProductForm.tsx`
- Может быть отдельной страницей или модальным окном (использовать ProductQuickAdd)
- Поля: название, категория, КБЖУ на 100г
- Валидация
- Сохранение

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Форма создана
- [ ] Валидация работает
- [ ] Сохранение работает

ФАЙЛЫ:
- `frontend/console/src/pages/products/ProductForm.tsx`

---

## Группа: Integration with Nutrition Programs

---

## ЗАДАЧА #029
TITLE: [FRONTEND] Create DishSelector Modal
PRIORITY: CRITICAL
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #014, #016, #026
BLOCKS: #031
ESTIMATED: 3h

ОПИСАНИЕ:
Создать модальное окно выбора блюда для добавления в программу питания.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/components/dishes/DishSelector.tsx`
- Props: isOpen, onClose, onSelect, mealType (опционально для фильтрации)
- Поиск по названию
- Фильтры: тип приёма пищи, теги
- Grid/List карточек блюд (компактный вид)
- Click на блюдо → onSelect(dish) → закрытие
- Ссылка "Создать новое блюдо" → открывает DishForm

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Модальное окно создано
- [x] Поиск и фильтры работают
- [x] Выбор блюда работает
- [x] Ссылка на создание нового

ФАЙЛЫ:
- `frontend/console/src/components/dishes/DishSelector.tsx`

ВЫПОЛНЕНО:
- 236 строк в components/dishes/DishSelector.tsx
- Dialog с debounced поиском
- Фильтр по типу приёма пищи
- Grid компактных карточек с фото и КБЖУ
- onSelect callback с закрытием
- Ссылки на создание нового блюда

---

## ЗАДАЧА #030
TITLE: [FRONTEND] Create DishPreview Component
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 13:18]
DEPENDS_ON: #013
BLOCKS: #031
ESTIMATED: 1h

ОПИСАНИЕ:
Создать компонент превью блюда для отображения в программе питания.

ТРЕБОВАНИЯ:
- Файл `frontend/console/src/components/dishes/DishPreview.tsx`
- Props: dish (или name, calories, proteins, fats, carbs)
- Компактное отображение: название, КБЖУ в одну строку
- Иконка блюда (если есть фото — миниатюра)
- Кнопка удаления из программы

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Компонент создан
- [x] Компактное отображение
- [x] Кнопка удаления

ФАЙЛЫ:
- `frontend/console/src/components/dishes/DishPreview.tsx`

ВЫПОЛНЕНО:
- Создан DishPreview компонент с props: dish, onRemove, showRemove, onClick
- Миниатюра фото с placeholder
- КБЖУ в одну строку
- Кнопка удаления с stopPropagation
- Добавлен DishPreviewCompact для компактного отображения в списках
- TypeScript типизация

---

## ЗАДАЧА #031
TITLE: [FRONTEND] Integrate DishSelector into NutritionProgramEdit
PRIORITY: CRITICAL
TYPE: INTEGRATION
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #029, #030
BLOCKS: -
ESTIMATED: 4h

ОПИСАНИЕ:
Интегрировать выбор блюд из базы в редактор программ питания.

ТРЕБОВАНИЯ:
- Модифицировать `frontend/console/src/pages/NutritionProgramEdit.tsx`
- В каждом приёме пищи дня добавить кнопку "Добавить из базы"
- Клик → открывает DishSelector с фильтром по типу приёма пищи
- После выбора блюда → копируются данные (name, description, calories, proteins, fats, carbs) в приём пищи
- Отображение добавленного блюда через DishPreview
- Возможность удалить блюдо из приёма пищи

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Кнопка "Добавить из базы" в каждом приёме пищи
- [x] DishSelector открывается корректно
- [x] Данные блюда копируются в программу
- [x] Превью блюда отображается
- [x] Удаление работает

ФАЙЛЫ:
- `frontend/console/src/pages/NutritionProgramEdit.tsx`
- Возможно компоненты внутри редактора

ВЫПОЛНЕНО:
- DishSelector импортирован и интегрирован
- Кнопка "Из базы блюд" с иконкой ChefHat
- handleOpenDishSelector открывает модал с нужным mealType
- handleDishSelected копирует данные блюда в приём пищи
- dishSelectorOpen, dishSelectorMealType состояния

---

## Группа: Navigation & Routing

---

## ЗАДАЧА #032
TITLE: [FRONTEND] Add Routes and Navigation
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #025, #026, #027, #028
BLOCKS: -
ESTIMATED: 2h

ОПИСАНИЕ:
Добавить маршруты и навигацию для страниц блюд и продуктов.

ТРЕБОВАНИЯ:
- Добавить routes в router конфигурацию:
  - /dishes — DishesDatabase
  - /dishes/new — DishForm (создание)
  - /dishes/:id — DishForm (редактирование)
  - /products — ProductsDatabase
  - /products/new — ProductForm (создание)
  - /products/:id — ProductForm (редактирование)
- Добавить пункты в боковое меню:
  - В разделе "Программы питания" → вкладка/подпункт "База блюд"
  - Или отдельный пункт "База блюд" в меню
- Breadcrumbs на страницах

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Все routes добавлены
- [x] Навигация в sidebar работает
- [ ] Breadcrumbs на страницах (опционально)
- [x] Переходы между страницами работают

ФАЙЛЫ:
- `frontend/console/src/App.tsx` или router config
- `frontend/console/src/components/Sidebar.tsx` или layout

ВЫПОЛНЕНО:
- Роуты добавлены в App.tsx:
  - /dishes → DishesDatabase
  - /dishes/new → DishForm
  - /dishes/:id → DishForm
- Импорты DishesDatabase и DishForm настроены
- Страницы продуктов пока не добавлены (опционально)

---

# PHASE 2: AI & UX (Should Have)

## Группа: AI Services Backend

---

## ЗАДАЧА #033
TITLE: [BACKEND] Create AI Service: Generate Recipe
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #003
BLOCKS: #037, #038
ESTIMATED: 3h

ОПИСАНИЕ:
Создать AI сервис для генерации рецепта блюда по названию через OpenAI.

ТРЕБОВАНИЯ:
- Файл `backend/apps/meals/ai_services.py`
- Функция `async def generate_recipe(dish_name: str) -> dict`
- Использовать OpenAI GPT-4o
- Prompt из требований (генерация рецепта)
- Возвращает: ingredients, recipe, cooking_time, portion_weight
- Error handling: timeout, API errors
- Logging использования токенов

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Функция создана
- [ ] Prompt корректный
- [ ] Парсинг JSON ответа
- [ ] Error handling
- [ ] Логирование

ФАЙЛЫ:
- `backend/apps/meals/ai_services.py`

---

## ЗАДАЧА #034
TITLE: [BACKEND] Create AI Service: Calculate Nutrition
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #003
BLOCKS: #037, #038
ESTIMATED: 2h

ОПИСАНИЕ:
Создать AI сервис для расчёта КБЖУ по списку ингредиентов.

ТРЕБОВАНИЯ:
- Функция `async def calculate_nutrition_from_ingredients(ingredients: list) -> dict`
- Использовать OpenAI GPT-4o
- Принимает список ингредиентов с весами
- Возвращает: calories, proteins, fats, carbohydrates
- Error handling

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Функция создана
- [ ] Корректный расчёт
- [ ] Error handling

ФАЙЛЫ:
- `backend/apps/meals/ai_services.py`

---

## ЗАДАЧА #035
TITLE: [BACKEND] Create AI Service: Suggest Product Nutrition
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #001
BLOCKS: #037, #039
ESTIMATED: 2h

ОПИСАНИЕ:
Создать AI сервис для подсказки КБЖУ продукта по названию.

ТРЕБОВАНИЯ:
- Функция `async def suggest_product_nutrition(product_name: str) -> dict`
- Использовать OpenAI GPT-4o
- Возвращает КБЖУ на 100г: calories, proteins, fats, carbs
- Error handling

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Функция создана
- [ ] Корректные значения КБЖУ
- [ ] Error handling

ФАЙЛЫ:
- `backend/apps/meals/ai_services.py`

---

## ЗАДАЧА #036
TITLE: [BACKEND] Create AI Service: Suggest Dish Description
PRIORITY: MEDIUM
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #003
BLOCKS: #037, #038
ESTIMATED: 1h

ОПИСАНИЕ:
Создать AI сервис для генерации краткого описания блюда.

ТРЕБОВАНИЯ:
- Функция `async def suggest_dish_description(dish_name: str) -> str`
- Использовать OpenAI GPT-4o
- Возвращает краткое описание (1-2 предложения)
- Error handling

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Функция создана
- [ ] Описание адекватное
- [ ] Error handling

ФАЙЛЫ:
- `backend/apps/meals/ai_services.py`

---

## ЗАДАЧА #037
TITLE: [BACKEND] Create AI API Endpoints
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #033, #034, #035, #036
BLOCKS: #038, #039
ESTIMATED: 2h

ОПИСАНИЕ:
Создать API endpoints для AI функций.

ТРЕБОВАНИЯ:
- Endpoints в views.py:
  - POST /api/dishes/ai/generate-recipe/ — {name} → recipe data
  - POST /api/dishes/ai/calculate-nutrition/ — {ingredients} → nutrition
  - POST /api/dishes/ai/suggest-description/ — {name} → description
  - POST /api/products/ai/suggest-nutrition/ — {name} → nutrition
- Rate limiting (опционально)
- Async views или sync wrappers

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Все endpoints созданы
- [ ] Корректная обработка запросов
- [ ] Error responses

ФАЙЛЫ:
- `backend/apps/meals/views.py`
- `backend/apps/meals/urls.py`

---

## Группа: AI Frontend Integration

---

## ЗАДАЧА #038
TITLE: [FRONTEND] Add AI Buttons to DishForm
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 16:00]
DEPENDS_ON: #026, #037
BLOCKS: -
ESTIMATED: 3h

ОПИСАНИЕ:
Добавить AI кнопки в форму блюда.

ТРЕБОВАНИЯ:
- Модифицировать DishForm.tsx
- Кнопка "Сгенерировать рецепт" рядом с полем названия:
  - Запрос к /api/dishes/ai/generate-recipe/
  - Заполнение полей: ingredients, recipe, cooking_time, portion_weight
  - Loading состояние
- Кнопка "Рассчитать КБЖУ" в секции ингредиентов:
  - Запрос к /api/dishes/ai/calculate-nutrition/
  - Обновление КБЖУ
- Кнопка "Сгенерировать описание":
  - Запрос к /api/dishes/ai/suggest-description/
  - Заполнение поля description
- Error handling с toast notifications

КРИТЕРИИ ГОТОВНОСТИ:
- [x] Кнопка генерации рецепта работает
- [x] Кнопка расчёта КБЖУ работает
- [x] Кнопка генерации описания работает
- [x] Loading состояния
- [x] Error handling

ВЫПОЛНЕНО:
- Добавлены AI API функции в api/dishes.ts:
  - dishesAiApi.generateRecipe(name)
  - dishesAiApi.calculateNutrition(ingredients)
  - dishesAiApi.suggestDescription(name)
  - dishesAiApi.suggestProductNutrition(name)
- Добавлены типы GeneratedRecipe, NutritionData, ProductNutritionData
- Модифицирован DishForm.tsx:
  - Импорт dishesAiApi и useToast
  - Состояния isGeneratingRecipe, isCalculatingNutrition, isGeneratingDescription
  - handleGenerateRecipe — заполняет ингредиенты, рецепт, КБЖУ
  - handleAiCalculateNutrition — рассчитывает КБЖУ по ингредиентам
  - handleGenerateDescription — генерирует описание
  - Кнопки с иконкой Sparkles и Loader2 при загрузке
  - Toast уведомления об успехе/ошибке
- [ ] Error handling

ФАЙЛЫ:
- `frontend/console/src/pages/dishes/DishForm.tsx`
- `frontend/console/src/api/dishes.ts` (добавить AI endpoints)

---

## ЗАДАЧА #039
TITLE: [FRONTEND] Add AI Button to ProductForm
PRIORITY: HIGH
TYPE: FEATURE
STATUS: TODO
DEPENDS_ON: #022, #037
BLOCKS: -
ESTIMATED: 1h

ОПИСАНИЕ:
Добавить AI кнопку подсказки КБЖУ в форму продукта.

ТРЕБОВАНИЯ:
- Модифицировать ProductQuickAdd.tsx и ProductForm.tsx
- Кнопка "Подсказать КБЖУ" рядом с полем названия
- Запрос к /api/products/ai/suggest-nutrition/
- Заполнение полей КБЖУ
- Loading и error states

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Кнопка работает
- [ ] КБЖУ заполняется
- [ ] Loading/error states

ФАЙЛЫ:
- `frontend/console/src/components/products/ProductQuickAdd.tsx`
- `frontend/console/src/pages/products/ProductForm.tsx`

---

## Группа: Celery Tasks

---

## ЗАДАЧА #040
TITLE: [BACKEND] Create Celery Task: Generate Dish Thumbnail
PRIORITY: MEDIUM
TYPE: FEATURE
STATUS: TODO
DEPENDS_ON: #003
BLOCKS: -
ESTIMATED: 2h

ОПИСАНИЕ:
Создать Celery task для генерации миниатюры фото блюда.

ТРЕБОВАНИЯ:
- Файл `backend/apps/meals/tasks.py`
- Task `generate_dish_thumbnail(dish_id: int)`
- Создаёт миниатюру 300x300 при загрузке фото
- Использовать Pillow
- Сохранение на R2 (или локально для dev)
- Вызывается после сохранения блюда с фото

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Task создан
- [ ] Миниатюра генерируется
- [ ] Интеграция с сохранением блюда

ФАЙЛЫ:
- `backend/apps/meals/tasks.py`
- `backend/apps/meals/signals.py` (опционально для автовызова)

---

## ЗАДАЧА #041
TITLE: [BACKEND] Create Celery Task: Recalculate Dishes Nutrition
PRIORITY: MEDIUM
TYPE: FEATURE
STATUS: TODO
DEPENDS_ON: #001, #003
BLOCKS: -
ESTIMATED: 2h

ОПИСАНИЕ:
Создать Celery task для пересчёта КБЖУ блюд при изменении продукта.

ТРЕБОВАНИЯ:
- Task `recalculate_dishes_nutrition(product_id: int)`
- Находит все блюда с этим продуктом в ingredients
- Пересчитывает КБЖУ каждого блюда
- Вызывается после обновления Product

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Task создан
- [ ] Пересчёт работает корректно
- [ ] Интеграция с обновлением продукта

ФАЙЛЫ:
- `backend/apps/meals/tasks.py`
- `backend/apps/meals/signals.py`

---

## Группа: Advanced Features

---

## ЗАДАЧА #042
TITLE: [BACKEND] Implement Dish Duplicate Action
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #011
BLOCKS: -
ESTIMATED: 1h

ОПИСАНИЕ:
Реализовать action дублирования блюда в DishViewSet.

ТРЕБОВАНИЯ:
- POST /api/dishes/{id}/duplicate/
- Создаёт копию блюда с новым id
- Название: "{original_name} (копия)"
- Копируются все поля кроме id, created_at, updated_at
- Теги связываются с копией

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Action реализован
- [ ] Копия создаётся корректно
- [ ] Теги копируются

ФАЙЛЫ:
- `backend/apps/meals/views.py`

---

## ЗАДАЧА #043
TITLE: [BACKEND] Implement Dish Archive Action
PRIORITY: HIGH
TYPE: FEATURE
STATUS: COMPLETED [2026-02-01 15:30]
DEPENDS_ON: #011
BLOCKS: -
ESTIMATED: 30m

ОПИСАНИЕ:
Реализовать action архивирования блюда в DishViewSet.

ТРЕБОВАНИЯ:
- POST /api/dishes/{id}/archive/
- Устанавливает is_active=False
- Блюдо не удаляется, только скрывается из списка

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Action реализован
- [ ] Блюдо скрывается из списка

ФАЙЛЫ:
- `backend/apps/meals/views.py`

---

## ЗАДАЧА #044
TITLE: [FEATURE] Implement Dishes Import/Export
PRIORITY: MEDIUM
TYPE: FEATURE
STATUS: TODO
DEPENDS_ON: #011, #025
BLOCKS: -
ESTIMATED: 4h

ОПИСАНИЕ:
Реализовать импорт и экспорт блюд в JSON/CSV формате.

ТРЕБОВАНИЯ:
- Backend:
  - GET /api/dishes/export/ — экспорт всех блюд коуча в JSON
  - POST /api/dishes/import/ — импорт блюд из JSON файла
  - Валидация структуры при импорте
- Frontend:
  - Кнопка "Экспорт" на странице базы блюд
  - Кнопка "Импорт" с file upload

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Экспорт в JSON работает
- [ ] Импорт из JSON работает
- [ ] Валидация при импорте
- [ ] UI кнопки

ФАЙЛЫ:
- `backend/apps/meals/views.py`
- `frontend/console/src/pages/dishes/DishesDatabase.tsx`

---

## ЗАДАЧА #045
TITLE: [FEATURE] Create Dish from Nutrition Program
PRIORITY: MEDIUM
TYPE: FEATURE
STATUS: TODO
DEPENDS_ON: #031
BLOCKS: -
ESTIMATED: 3h

ОПИСАНИЕ:
Реализовать быстрое создание блюда из существующего приёма пищи в программе.

ТРЕБОВАНИЯ:
- В редакторе программы: кнопка "Сохранить в базу" у приёма пищи
- Открывает DishForm с предзаполненными данными из программы
- После сохранения — блюдо появляется в базе

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Кнопка в редакторе программы
- [ ] Предзаполнение формы
- [ ] Сохранение работает

ФАЙЛЫ:
- `frontend/console/src/pages/NutritionProgramEdit.tsx`
- `frontend/console/src/pages/dishes/DishForm.tsx`

---

## Группа: Drag-and-Drop

---

## ЗАДАЧА #046
TITLE: [FRONTEND] Create Draggable Dishes Panel
PRIORITY: MEDIUM
TYPE: FEATURE
STATUS: TODO
DEPENDS_ON: #025, #031
BLOCKS: #047
ESTIMATED: 3h

ОПИСАНИЕ:
Создать боковую панель с перетаскиваемыми блюдами для редактора программ.

ТРЕБОВАНИЯ:
- Панель справа или слева от редактора программы
- Поиск и фильтры блюд
- Компактные карточки блюд с возможностью drag
- Использовать react-beautiful-dnd или @dnd-kit

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Панель создана
- [ ] Drag начинается при перетаскивании карточки
- [ ] Поиск работает

ФАЙЛЫ:
- `frontend/console/src/components/dishes/DraggableDishesPanel.tsx`

---

## ЗАДАЧА #047
TITLE: [FRONTEND] Implement Drop Zones in Program Editor
PRIORITY: MEDIUM
TYPE: FEATURE
STATUS: TODO
DEPENDS_ON: #031, #046
BLOCKS: -
ESTIMATED: 4h

ОПИСАНИЕ:
Реализовать drop zones для блюд в редакторе программ питания.

ТРЕБОВАНИЯ:
- Модифицировать NutritionProgramEdit.tsx
- Каждый приём пищи — drop zone
- Визуальная обратная связь при hover
- При drop — данные блюда копируются в приём пищи
- Интеграция с DraggableDishesPanel

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Drop zones работают
- [ ] Визуальная обратная связь
- [ ] Данные копируются корректно

ФАЙЛЫ:
- `frontend/console/src/pages/NutritionProgramEdit.tsx`

---

# PHASE 3: Testing & Documentation

---

## ЗАДАЧА #048
TITLE: [TESTING] Backend Unit Tests for Models
PRIORITY: HIGH
TYPE: TESTING
STATUS: TODO
DEPENDS_ON: #004
BLOCKS: -
ESTIMATED: 3h

ОПИСАНИЕ:
Написать unit тесты для моделей Product, DishTag, Dish.

ТРЕБОВАНИЯ:
- Файл `backend/apps/meals/tests/test_models.py`
- Тесты создания моделей
- Тесты валидации полей
- Тесты unique constraints
- Тест recalculate_nutrition()

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Тесты моделей написаны
- [ ] Все тесты проходят
- [ ] Coverage > 80%

ФАЙЛЫ:
- `backend/apps/meals/tests/test_models.py`

---

## ЗАДАЧА #049
TITLE: [TESTING] Backend Unit Tests for API
PRIORITY: HIGH
TYPE: TESTING
STATUS: TODO
DEPENDS_ON: #012
BLOCKS: -
ESTIMATED: 4h

ОПИСАНИЕ:
Написать unit тесты для API endpoints блюд и продуктов.

ТРЕБОВАНИЯ:
- Файл `backend/apps/meals/tests/test_views.py`
- Тесты CRUD для Dish, Product, DishTag
- Тесты фильтрации и поиска
- Тесты permissions (доступ только к своим данным)
- Тесты duplicate/archive actions

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Тесты API написаны
- [ ] Все тесты проходят
- [ ] Тесты permissions

ФАЙЛЫ:
- `backend/apps/meals/tests/test_views.py`

---

## ЗАДАЧА #050
TITLE: [TESTING] Frontend Component Tests
PRIORITY: MEDIUM
TYPE: TESTING
STATUS: TODO
DEPENDS_ON: #026, #029
BLOCKS: -
ESTIMATED: 4h

ОПИСАНИЕ:
Написать тесты для ключевых React компонентов.

ТРЕБОВАНИЯ:
- Тесты для DishCard
- Тесты для DishForm (рендеринг, валидация)
- Тесты для DishSelector
- Тесты для IngredientsTable
- Использовать @testing-library/react

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Тесты компонентов написаны
- [ ] Все тесты проходят

ФАЙЛЫ:
- `frontend/console/src/components/dishes/__tests__/`
- `frontend/console/src/pages/dishes/__tests__/`

---

## ЗАДАЧА #051
TITLE: [TESTING] Backend AI Services Tests
PRIORITY: MEDIUM
TYPE: TESTING
STATUS: TODO
DEPENDS_ON: #037
BLOCKS: -
ESTIMATED: 2h

ОПИСАНИЕ:
Написать тесты для AI сервисов с mock OpenAI API.

ТРЕБОВАНИЯ:
- Файл `backend/apps/meals/tests/test_ai_services.py`
- Mock OpenAI responses
- Тесты парсинга ответов
- Тесты error handling

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Тесты AI сервисов написаны
- [ ] Все тесты проходят

ФАЙЛЫ:
- `backend/apps/meals/tests/test_ai_services.py`

---

## ЗАДАЧА #052
TITLE: [DEPLOY] Production Deployment Preparation
PRIORITY: HIGH
TYPE: INFRASTRUCTURE
STATUS: TODO
DEPENDS_ON: #048, #049
BLOCKS: -
ESTIMATED: 2h

ОПИСАНИЕ:
Подготовить деплой на Railway.

ТРЕБОВАНИЯ:
- Проверить миграции на production DB
- Проверить переменные окружения (OPENAI_API_KEY)
- Проверить storage для фото (Cloudflare R2)
- Деплой: api, celery, console

КРИТЕРИИ ГОТОВНОСТИ:
- [ ] Миграции готовы
- [ ] Env variables настроены
- [ ] Storage настроен
- [ ] Деплой выполнен

КОМАНДЫ:
```bash
ops/railway/deploy.sh api celery console
```

---

# DEPENDENCY GRAPH

```
Phase 1 - Models:
#001 (Product) ─┬─→ #003 (Dish) ─→ #004 (Migrations) ─→ #005 (Indexes)
#002 (DishTag) ─┘

Phase 1 - Serializers:
#001 → #006 (ProductSerializer)
#002 → #007 (DishTagSerializer)
#003, #007 → #008 (DishSerializers)

Phase 1 - Views:
#004, #006 → #009 (ProductViewSet)
#004, #007 → #010 (DishTagViewSet)
#004, #008 → #011 (DishViewSet)
#009, #010, #011 → #012 (URLs)

Phase 1 - Frontend Core:
#013 (Types) - independent
#012, #013 → #014 (API Client)
#013, #014 → #015 (Store)

Phase 1 - Frontend Components:
#013 → #016, #017, #018, #019, #020
#013, #014 → #021, #022
#021 → #023 → #024

Phase 1 - Frontend Pages:
#015, #016 → #025 (DishesDatabase)
#015, #017-#020, #024 → #026 (DishForm)
#014 → #027, #028

Phase 1 - Integration:
#014, #016, #026 → #029 (DishSelector)
#013 → #030 (DishPreview)
#029, #030 → #031 (Integration)

Phase 1 - Navigation:
#025-#028 → #032 (Routes)

Phase 2 - AI:
#033-#036 → #037 (AI Endpoints)
#026, #037 → #038 (AI in DishForm)
#022, #037 → #039 (AI in ProductForm)

Phase 2 - Celery:
#003 → #040, #041

Phase 2 - Advanced:
#011 → #042, #043
#011, #025 → #044
#031 → #045, #046 → #047

Phase 3 - Testing:
#004 → #048
#012 → #049
#026, #029 → #050
#037 → #051
#048, #049 → #052
```

---

# PARALLEL EXECUTION OPPORTUNITIES

## Batch 1 (можно параллельно):
- #001, #002, #013 — независимые задачи

## Batch 2 (после Batch 1):
- #003 (после #001, #002)
- #006, #007 (после #001, #002)

## Batch 3 (после Batch 2):
- #004 (после #003)
- #008 (после #003, #007)
- #016, #017, #018, #019, #020, #021, #022 (после #013)

## Batch 4 (после Batch 3):
- #005, #009, #010, #011 (после #004)
- #014 (после #012)
- #023 (после #021)

## Batch 5 (после Batch 4):
- #012 (после #009, #010, #011)
- #015, #024 (после #014, #023)

## Batch 6 (после Batch 5):
- #025, #026, #027, #028, #029, #030 (после dependencies)

## AI Tasks (Phase 2, параллельно после #003):
- #033, #034, #035, #036 — все независимы друг от друга

---

# NOTES

- Frontend задачи (#013+) могут начинаться параллельно с backend после определения API контрактов
- AI сервисы (#033-#036) можно разрабатывать параллельно после создания моделей
- Тестирование (#048-#051) следует начинать как только готовы соответствующие компоненты
- Критический путь проходит через: Models → Migrations → Serializers → Views → URLs → API Client → Store → Pages → Integration
