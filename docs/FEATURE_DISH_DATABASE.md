# Функция: База данных блюд коуча

## Обзор

База данных блюд позволяет коучу создавать и хранить собственную библиотеку блюд с рецептами, ингредиентами и КБЖУ. Блюда используются при составлении программ питания для клиентов.

---

## Модели данных

### Product — Продукт

Базовый продукт с КБЖУ на 100 грамм. Продукты переиспользуются как ингредиенты в разных блюдах.

```python
Product
  - coach (FK -> Coach)           # Владелец продукта
  - name (CharField, max=255)     # Название продукта

  # КБЖУ на 100 грамм
  - calories_per_100g (Decimal)   # Калории
  - proteins_per_100g (Decimal)   # Белки
  - fats_per_100g (Decimal)       # Жиры
  - carbs_per_100g (Decimal)      # Углеводы

  - category (CharField)          # Категория продукта
  - is_verified (Boolean)         # Подтверждено после AI-подсказки

  - created_at, updated_at
```

**Категории продуктов:**
| Код | Название |
|-----|----------|
| dairy | Молочные продукты |
| meat | Мясо |
| fish | Рыба и морепродукты |
| vegetables | Овощи |
| fruits | Фрукты |
| grains | Крупы и злаки |
| nuts | Орехи и семена |
| oils | Масла и жиры |
| spices | Специи и приправы |
| other | Прочее |

**Методы:**
- `get_nutrition_for_weight(weight_grams)` — рассчитать КБЖУ для заданного веса

**Ограничения:**
- `unique_together: ['coach', 'name']` — уникальность названия в рамках коуча
- Все числовые поля КБЖУ >= 0

---

### DishTag — Тег блюда

Теги для категоризации и фильтрации блюд (например: "Низкоуглеводные", "Веганские", "Быстрые рецепты").

```python
DishTag
  - coach (FK -> Coach)           # Владелец тега
  - name (CharField, max=50)      # Название тега
  - color (CharField, max=7)      # HEX-код цвета (#3B82F6)

  - created_at, updated_at
```

**Ограничения:**
- `unique_together: ['coach', 'name']` — уникальность названия в рамках коуча
- Цвет по умолчанию: `#3B82F6` (синий)

---

### Dish — Блюдо

Полноценное блюдо с рецептом, ингредиентами и медиа.

```python
Dish
  - coach (FK -> Coach)           # Владелец блюда

  # Основная информация
  - name (CharField, max=255)     # Название блюда
  - description (TextField)       # Краткое описание
  - recipe (TextField)            # Рецепт (поддержка Markdown)

  # КБЖУ порции
  - portion_weight (Integer)      # Вес порции в граммах
  - calories (Decimal)            # Калории
  - proteins (Decimal)            # Белки
  - fats (Decimal)                # Жиры
  - carbohydrates (Decimal)       # Углеводы

  # Время
  - cooking_time (Integer, null)  # Время приготовления в минутах

  # Медиа
  - photo (ImageField)            # Фото блюда (dishes/%Y/%m/)
  - video_url (URLField)          # Ссылка на видео-рецепт

  # JSON поля
  - ingredients (JSONField)       # Список ингредиентов
  - shopping_links (JSONField)    # Ссылки на покупку
  - meal_types (JSONField)        # Типы приёмов пищи

  # Категоризация
  - tags (M2M -> DishTag)         # Теги блюда
  - is_active (Boolean)           # Активно/скрыто

  - created_at, updated_at
```

**Формат ingredients (JSON):**
```json
[
  {
    "product_id": 1,              // ID продукта (опционально)
    "name": "Куриная грудка",     // Название
    "weight": 150,                // Вес в граммах
    "calories": 247.5,            // Калории для этого веса
    "proteins": 46.5,             // Белки
    "fats": 5.25,                 // Жиры
    "carbohydrates": 0            // Углеводы
  }
]
```

**Формат shopping_links (JSON):**
```json
[
  {"title": "iHerb", "url": "https://iherb.com/..."},
  {"title": "Ozon", "url": "https://ozon.ru/..."}
]
```

**Типы приёмов пищи (meal_types):**
| Код | Название |
|-----|----------|
| breakfast | Завтрак |
| snack1 | Перекус 1 |
| lunch | Обед |
| snack2 | Перекус 2 |
| dinner | Ужин |

**Методы:**
- `recalculate_nutrition()` — пересчитать КБЖУ на основе ингредиентов

---

## API Endpoints

### Products API (ProductViewSet)

**Файл:** `apps/meals/views.py:371`

```
GET    /api/products/              # Список продуктов коуча
POST   /api/products/              # Создать продукт
GET    /api/products/{id}/         # Детали продукта
PUT    /api/products/{id}/         # Обновить продукт
PATCH  /api/products/{id}/         # Частичное обновление
DELETE /api/products/{id}/         # Удалить продукт
GET    /api/products/search/?q=... # Поиск для автокомплита (до 10 результатов)
```

**Фильтры:**
- `?search=молоко` — поиск по названию
- `?category=dairy` — фильтр по категории
- `?is_verified=true` — только проверенные
- `?ordering=name,-created_at` — сортировка

**Пагинация:** 50 элементов на страницу (max 100)

---

### DishTags API (DishTagViewSet)

**Файл:** `apps/meals/views.py:436`

```
GET    /api/dish-tags/             # Список тегов коуча
POST   /api/dish-tags/             # Создать тег
GET    /api/dish-tags/{id}/        # Детали тега
PUT    /api/dish-tags/{id}/        # Обновить тег
PATCH  /api/dish-tags/{id}/        # Частичное обновление
DELETE /api/dish-tags/{id}/        # Удалить тег
```

**Сортировка:** по умолчанию по `name`

---

### Dishes API (DishViewSet)

**Файл:** `apps/meals/views.py:465`

```
GET    /api/dishes/                # Список блюд коуча
POST   /api/dishes/                # Создать блюдо
GET    /api/dishes/{id}/           # Детали блюда
PUT    /api/dishes/{id}/           # Обновить блюдо
PATCH  /api/dishes/{id}/           # Частичное обновление
DELETE /api/dishes/{id}/           # Удалить блюдо
POST   /api/dishes/{id}/duplicate/ # Дублировать блюдо
POST   /api/dishes/{id}/archive/   # Архивировать блюдо
```

**Фильтры:**
- `?search=курица` — поиск по названию и описанию
- `?tags=1,2,3` — фильтр по тегам (OR логика)
- `?meal_type=breakfast` — фильтр по типу приёма пищи
- `?is_active=true` — фильтр по статусу
- `?show_archived=true` — показать архивные (по умолчанию скрыты)
- `?ordering=-updated_at,name` — сортировка

**Пагинация:** 20 элементов на страницу (max 100)

**Сериализаторы:**
- `DishListSerializer` — компактный для списка
- `DishDetailSerializer` — полный для detail/create/update

---

## UI Консоли коуча

### Страница "База данных блюд"

**Путь:** `/dishes`

**Компоненты:**
1. Список блюд с фильтрацией и поиском
2. Карточка блюда (DishCard) с:
   - Фото
   - Название
   - КБЖУ
   - Теги
   - Время приготовления

### Страница "Форма блюда"

**Путь:** `/dishes/new`, `/dishes/{id}/edit`

**Поля формы:**
1. Основная информация (название, описание)
2. Редактор ингредиентов с автоподсказкой из базы продуктов
3. Рецепт (Markdown редактор)
4. Медиа (фото, видео URL)
5. Теги и типы приёмов пищи
6. Автоматический расчёт КБЖУ

---

## Интеграция с программами питания

Блюда из базы данных используются при составлении программ питания:

1. **DishSelector Modal** — модальное окно выбора блюда из базы
2. **Автоподстановка** — при добавлении блюда в программу автоматически подставляются КБЖУ
3. **Связь с NutritionProgramDay** — блюда привязываются к дням программы

---

## Миграции

**Миграция:** `0006_add_product_dishtag_dish.py`

Создаёт таблицы:
- `products`
- `dish_tags`
- `dishes`
- `dishes_tags` (M2M связь)

---

## Статус реализации

### Backend

| Компонент | Статус | Файл |
|-----------|--------|------|
| Модель Product | ✅ Готово | `apps/meals/models.py:37` |
| Модель DishTag | ✅ Готово | `apps/meals/models.py:132` |
| Модель Dish | ✅ Готово | `apps/meals/models.py:190` |
| Миграции | ✅ Готово | `apps/meals/migrations/0006_*.py` |
| ProductSerializer | ✅ Готово | `apps/meals/serializers.py:108` |
| DishTagSerializer | ✅ Готово | `apps/meals/serializers.py:159` |
| DishListSerializer | ✅ Готово | `apps/meals/serializers.py:199` |
| DishDetailSerializer | ✅ Готово | `apps/meals/serializers.py:223` |
| ProductViewSet | ✅ Готово | `apps/meals/views.py:371` |
| DishTagViewSet | ✅ Готово | `apps/meals/views.py:436` |
| DishViewSet | ✅ Готово | `apps/meals/views.py:465` |
| URL маршруты API | ✅ Готово | `apps/meals/urls.py` |

### Frontend

| Компонент | Статус | Файл |
|-----------|--------|------|
| TypeScript типы | ✅ Готово | `console/src/types/dishes.ts` |
| API клиент | ✅ Готово | `console/src/api/dishes.ts` |
| Zustand store | ✅ Готово | `console/src/stores/dishes.ts` |
| Компонент DishCard | ✅ Готово | `console/src/components/dishes/DishCard.tsx` |
| Страница списка блюд | ✅ Готово | `console/src/pages/dishes/DishesDatabase.tsx` |
| Страница формы блюда | ✅ Готово | `console/src/pages/dishes/DishForm.tsx` |
| DishSelector Modal | 🔄 В процессе | |
| Routes и Navigation | ✅ Готово | `console/src/App.tsx:63-65` |

---

## TypeScript типы

**Файл:** `frontend/console/src/types/dishes.ts`

### Константы

```typescript
type MealType = 'breakfast' | 'snack1' | 'lunch' | 'snack2' | 'dinner'
type ProductCategory = 'dairy' | 'meat' | 'fish' | 'vegetables' | 'fruits' |
                       'grains' | 'nuts' | 'oils' | 'spices' | 'other'
```

### Основные интерфейсы

| Интерфейс | Описание |
|-----------|----------|
| `Product` | Продукт из базы данных |
| `ProductFormData` | Данные формы создания/редактирования продукта |
| `ProductListParams` | Параметры запроса списка продуктов |
| `DishTag` | Тег для категоризации блюд |
| `DishTagFormData` | Данные формы создания/редактирования тега |
| `Ingredient` | Ингредиент в блюде |
| `ShoppingLink` | Ссылка на покупку продукта |
| `DishListItem` | Компактный вид блюда для списка |
| `Dish` | Полная информация о блюде |
| `DishFormData` | Данные формы создания/редактирования блюда |
| `DishListParams` | Параметры запроса списка блюд |
| `DishFilters` | Фильтры для UI |
| `PaginatedResponse<T>` | Обёртка для пагинированных ответов |

---

## API клиент

**Файл:** `frontend/console/src/api/dishes.ts`

### dishesApi

```typescript
dishesApi.list(params?)      // Список блюд с пагинацией
dishesApi.get(id)            // Получить блюдо по ID
dishesApi.create(formData)   // Создать блюдо
dishesApi.update(id, data)   // Обновить блюдо
dishesApi.delete(id)         // Удалить блюдо
dishesApi.duplicate(id)      // Дублировать блюдо
dishesApi.archive(id)        // Архивировать блюдо
```

### productsApi

```typescript
productsApi.list(params?)    // Список продуктов с пагинацией
productsApi.search(query)    // Поиск для автокомплита
productsApi.get(id)          // Получить продукт по ID
productsApi.create(formData) // Создать продукт
productsApi.update(id, data) // Обновить продукт
productsApi.delete(id)       // Удалить продукт
```

### dishTagsApi

```typescript
dishTagsApi.list()           // Получить все теги
dishTagsApi.create(formData) // Создать тег
dishTagsApi.update(id, data) // Обновить тег
dishTagsApi.delete(id)       // Удалить тег
```

### Особенности

- Автоматическая обработка `FormData` при загрузке фото
- Поддержка частичных обновлений через PATCH
- Типизированные ответы API

---

## Zustand Store

**Файл:** `frontend/console/src/stores/dishes.ts`

### Состояние

```typescript
interface DishesState {
  // Данные
  dishes: DishListItem[]
  selectedDish: Dish | null
  tags: DishTag[]
  products: Product[]

  // Фильтры
  filters: DishFilters

  // Пагинация
  page: number
  hasMore: boolean
  totalCount: number

  // Состояние загрузки
  isLoading: boolean
  isLoadingMore: boolean
  error: string | null
}
```

### Actions — Блюда

| Action | Описание |
|--------|----------|
| `fetchDishes(reset?)` | Загрузить список блюд |
| `loadMoreDishes()` | Подгрузить следующую страницу |
| `fetchDish(id)` | Загрузить блюдо по ID |
| `createDish(data)` | Создать блюдо |
| `updateDish(id, data)` | Обновить блюдо |
| `deleteDish(id)` | Удалить блюдо |
| `duplicateDish(id)` | Дублировать блюдо |
| `archiveDish(id)` | Архивировать блюдо |

### Actions — Фильтры

| Action | Описание |
|--------|----------|
| `setFilters(filters)` | Установить фильтры (автоперезагрузка) |
| `resetFilters()` | Сбросить фильтры |

### Actions — Теги и Продукты

| Action | Описание |
|--------|----------|
| `fetchTags()` | Загрузить все теги |
| `createTag(data)` / `updateTag()` / `deleteTag()` | CRUD тегов |
| `fetchProducts()` | Загрузить все продукты |
| `searchProducts(query)` | Поиск для автокомплита |
| `createProduct()` / `updateProduct()` / `deleteProduct()` | CRUD продуктов |

### Особенности

- **Оптимистичные обновления** при удалении/архивировании с откатом при ошибке
- **Infinite scroll** через `loadMoreDishes()`
- **Автоматическая перезагрузка** при изменении фильтров

---

## Компонент DishCard

**Файл:** `frontend/console/src/components/dishes/DishCard.tsx`

### Props

```typescript
interface DishCardProps {
  dish: DishListItem
  onClick?: () => void
  onEdit?: () => void
  onDuplicate?: () => void
  onArchive?: () => void
}
```

### Отображаемая информация

- Фото блюда (или плейсхолдер)
- Время приготовления (badge)
- Название блюда
- КБЖУ (калории, белки, жиры, углеводы)
- Вес порции
- Теги (до 3, остальные как "+N")
- Типы приёмов пищи

### Действия (dropdown)

- Редактировать
- Дублировать
- Архивировать

### Стилизация

- Hover-эффекты (тень, подсветка границы)
- Скрытое dropdown меню (появляется при hover)
- Цветные теги с полупрозрачным фоном

---

## Страница DishesDatabase

**Файл:** `frontend/console/src/pages/dishes/DishesDatabase.tsx`

**Путь:** `/dishes`

### Функциональность

- **Список блюд** в виде сетки карточек (DishCard)
- **Поиск** с debounce 300ms
- **Фильтрация:**
  - По типу приёма пищи
  - По тегам
  - Показать/скрыть архивные
- **Infinite scroll** — подгрузка при скролле
- **Действия:** создать, редактировать, дублировать, архивировать

### UI компоненты

- Header с заголовком и счётчиком
- Панель фильтров с Select и Input
- Сетка карточек блюд
- Индикатор загрузки

---

## Страница DishForm

**Файл:** `frontend/console/src/pages/dishes/DishForm.tsx`

**Путь:** `/dishes/new`, `/dishes/:id`

### Функциональность

- **Создание** нового блюда
- **Редактирование** существующего блюда
- **Загрузка фото** с превью
- **Редактор ингредиентов:**
  - Добавление/удаление ингредиентов
  - КБЖУ для каждого ингредиента
  - Автоподсчёт общих КБЖУ
- **Выбор тегов** (чекбоксы)
- **Выбор типов приёмов пищи** (чекбоксы)
- **Ссылки на покупку** (title + URL)

### Поля формы

| Поле | Тип | Описание |
|------|-----|----------|
| name | text | Название блюда |
| description | textarea | Описание |
| recipe | textarea | Рецепт (Markdown) |
| portion_weight | number | Вес порции (г) |
| calories, proteins, fats, carbohydrates | number | КБЖУ |
| cooking_time | number | Время приготовления (мин) |
| video_url | url | Ссылка на видео |
| photo | file | Фото блюда |
| meal_types | checkboxes | Типы приёмов пищи |
| tag_ids | checkboxes | Теги |
| ingredients | array | Список ингредиентов |
| shopping_links | array | Ссылки на покупку |

---

## Маршруты (Routes)

**Файл:** `frontend/console/src/App.tsx`

| Путь | Компонент | Описание |
|------|-----------|----------|
| `/dishes` | `DishesDatabase` | Список блюд |
| `/dishes/new` | `DishForm` | Создание блюда |
| `/dishes/:id` | `DishForm` | Редактирование блюда |

---

## Безопасность

### Реализованные меры

1. **Валидация HEX-цвета в DishTag** ✅
   - Regex валидация: `^#[0-9A-Fa-f]{6}$`
   - Приведение к верхнему регистру
   - Файл: `serializers.py:167`

2. **Валидация JSONField ingredients** ✅
   - Проверка структуры через `DishIngredientSerializer`
   - Валидация всех числовых полей >= 0
   - Файл: `serializers.py:260`

3. **Валидация JSONField shopping_links** ✅
   - Проверка структуры через `ShoppingLinkSerializer`
   - Валидация URL формата
   - Файл: `serializers.py:269`

4. **Валидация meal_types** ✅
   - Проверка допустимых значений из MEAL_TYPES
   - Файл: `serializers.py:278`

### Открытые задачи

1. **[SECURITY] Валидация ImageField upload** (#23)
   - Проверка типа файла
   - Ограничение размера
   - Санитизация имени файла

---

*Документ создан: 2026-02-01*
*Последнее обновление: 2026-02-01*
