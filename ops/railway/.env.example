# Copy to ops/railway/.env and fill your token(s).
# Any single token is enough; priority is RAILWAY_API_TOKEN > RAILWAY_TOKEN > RAILWAY_PROJECT_TOKEN.
RAILWAY_API_TOKEN=
RAILWAY_TOKEN=
RAILWAY_PROJECT_TOKEN=
